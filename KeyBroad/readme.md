**这个KeyBroad文件需要配合硬件的引脚和Timer中断使用**

1. 硬件引脚配置：
这个代码使用了矩阵键盘，即H1 - H4的行和V1 - V4的列
+ H1 到 H4配置为输出，初始为低电平，悬空
+ V1 到 V4配置为输入，为上拉输入

2. 定时器配置
+ 为了能不阻塞主程序，我们需要配置一个10ms的周期计数的计时器，然后开启他的中断
+ 在主程序初始化的时候，要开启NVIC中断，并且开启Timer，不然按键无法起作用
